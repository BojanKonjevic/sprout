"""Celery tasks.

Usage:
    from (( pkg_name )).tasks import add
    result = add.delay(1, 2)
    print(result.get(timeout=10))  # 3
"""

from .worker import celery_app


@celery_app.task
def add(x: int, y: int) -> int:
    """Example task — replace or delete."""
    return x + y
